[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node insights-server.js"
healthcheckPath = "/api/clients"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
PORT = "3000"